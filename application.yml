global:
  message: Welcome to global Microservices Environment!!
logging:
 level:
  root: INFO
 eureka:
 client:
   serviceUrl:
     defaultZone: http://eureka-container:8761/eureka
 management:
  endpoints:
   web:
    exposure:
     include: health,Infi,refresh
  endpoint:
      health:
       show-details: always
  
